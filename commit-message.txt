Add per-switch naming and scheduling to Multi-Switch devices

- Name and optionally schedule each switch individually in create/edit
- Extend ScheduleMonitor to enforce per-switch schedules, not just device-level
- Make the create/edit device dialog scrollable to fit the new fields