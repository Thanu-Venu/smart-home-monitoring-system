<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Kept in sync with ui/theme/Color.kt's LightPrimary/LightBackground.
        Used by the native pre-Compose theme (themes.xml) — the status
        bar and window background before Compose's first frame draws —
        and by the adaptive launcher icon background.
    -->
    <color name="brand_primary">#0058CC</color>
    <color name="brand_primary_dark">#001A41</color>
    <color name="brand_background">#FBFCFF</color>
</resources>
